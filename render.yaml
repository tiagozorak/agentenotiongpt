services:
  - type: web
    name: agentenotiongpt
    runtime: python
    buildCommand: "pip install --upgrade pip && pip install uvicorn && pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"

